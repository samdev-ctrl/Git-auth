GITHUB_CLIENT_ID=your_github_client_id_here
GITHUB_CLIENT_SECRET=your_github_client_secret_here

# Single authentication token (used for both authentication AND state signing)
# Generate token with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
OAUTH_SERVICE_TOKEN=your_oauth_service_token_here

APP_REDIRECT_FALLBACK=http://localhost:3000
PORT=3000
